# Anthropic API key — powers the connection-engine LLM.
# WITHOUT this, Tapsa still runs end-to-end using a heuristic fallback
# (it just won't pick the clever, surprising connections).
ANTHROPIC_API_KEY=

# Optional: override the Claude model used for generation.
# TAPSA_MODEL=claude-3-5-sonnet-latest

# Optional: cold (uncached) generations allowed per IP per minute.
# TAPSA_COLD_LIMIT=8

# Public site URL — used for absolute OG image + share URLs in production.
# NEXT_PUBLIC_SITE_URL=https://tapsa.ai
